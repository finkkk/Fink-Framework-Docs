# 基础系统（Core Systems）

基础系统提供不依赖具体玩法的运行时服务：单例、对象池、生命周期回调、计时器、音效、场景切换和事件分发。

这些系统共享 `FinkFramework.Runtime` 命名空间下的管理器入口。需要长期持有的监听、计时器和加载句柄，应在业务对象销毁或场景退出时主动释放。

基础系统是框架运行时的通用能力集合，包含单例、对象池、生命周期、定时、音效、场景切换与事件等模块。输入设备检测和键鼠事件绑定已归入独立的[输入系统](/input-system/)文档。

请从左侧菜单选择具体模块开始阅读。
